{
  "name": "shoplink",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:generate": "pnpm --filter @shoplink/db generate",
    "db:migrate": "pnpm --filter @shoplink/db migrate:dev",
    "db:push": "pnpm --filter @shoplink/db db:push",
    "db:seed": "pnpm --filter @shoplink/db seed",
    "api:dev": "pnpm --filter @shoplink/api dev",
    "admin:dev": "pnpm --filter @shoplink/web-admin dev",
    "marketing:dev": "pnpm --filter @shoplink/web-marketing dev",
    "postinstall": "turbo run generate"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.5",
    "prettier": "^3.2.5",
    "@types/node": "^20.12.7"
  }
}
